# Salin file ini menjadi .env dan sesuaikan nilainya
# Jika kosong/tidak ada, soal akan di-generate langsung di browser (tanpa backend)

# URL backend Express (development)
VITE_API_URL=http://localhost:3001/api

# Contoh jika deploy backend ke Render:
# VITE_API_URL=https://cpns-api.onrender.com/api
